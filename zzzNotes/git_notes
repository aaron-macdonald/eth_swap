//===========================
// Branches
//===========================
master
buyTokens
sellTokens

//===========================
// Basic Git Commands
//===========================
git  status

git add .

git commit -m "commit comment"

git status

git push --set-upstream origin buyTokens //branch name

// To merge the branch into master - after commiting and pushing
git checkout master
git merge branchName